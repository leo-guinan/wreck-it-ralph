# Game: Ship Small Features

Goal: Ship small, testable UI/product increments quickly without missing observations.

Win condition:
- All quests in QUESTS.json have passes=true
- Referee checks pass every turn (typecheck + tests + UI proof when requested)

Turn loop:
Observe → Implement ONE quest → Verify → Bank (commit) → Update memory.
