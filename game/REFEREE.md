# Referee Requirements

Required each turn:
- npm run typecheck
- npm test

If the quest includes "uiProof": true:
- Start dev server (if not already) and capture a screenshot to tmp/ui-proof.png

Banking:
- Only commit if all required checks pass.
